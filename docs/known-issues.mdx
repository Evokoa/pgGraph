# Known Issues

<Callout type="warning">
pgGraph is currently alpha software. Do not use it for production workloads
without your own validation, backup, rollback, and load-testing plan.
</Callout>

<Callout type="info">
pgGraph is built around a simple launch standard: the most critical correctness,
durability, safety, and operational paths should be solid first. There is always
another way to optimize a graph engine, especially inside PostgreSQL, so we are
tracking gaps in those paths explicitly and continuously hardening them in the
open before recommending production use.
</Callout>

This page documents known alpha limitations, next-update hardening work, and
lower-priority follow-up items. The list is ranked by expected user impact, not
by implementation difficulty.

This page is the authoritative register for known limitations and correctness,
operational, performance, or maintainability risks. Future product direction,
new APIs, and feature expansion belong in [Roadmap](./roadmap). Completed
hardening work remains documented in historical release notes and the resolved
summaries below.

Low-level PostgreSQL extension work involves real tradeoffs. Some optimizations
make a query faster by keeping more data in memory, while others reduce memory
pressure by doing more work per request. pgGraph has to weigh those choices
carefully because it runs inside PostgreSQL backend processes rather than a
separate service with its own resource envelope.

## Active Next Update Scope

Only unresolved or deliberately pending rows appear in the active sections.
Resolved work is preserved near the end of this page and in release notes.

### Sync, Jobs, and Operator Semantics

| ID | Issue | Status | Notes |
|---|---|---|---|
| KI-004 | pgGraph has a scheduler-safe `graph.run_scheduled_maintenance()` entry point, but no always-on internal scheduler for trigger-sync apply and overlay compaction in non-Docker installs. | Explicit implementation/policy decision required | Either ship a supervised bounded per-database scheduler with cross-backend/crash tests, or declare `pg_cron`/external scheduling the final supported contract and close the issue with complete setup, health, and failure guidance. |

## P0: Correctness, Security, Durability, and Resource Bounds

These are the highest-priority public follow-ups because they affect operators
running larger graphs or higher sync volume, and because some findings can
change observable graph correctness or PostgreSQL security boundaries.

`graph.memory_profile()` remains useful for capacity planning, but visibility is
not enforcement. `graph.memory_limit_mb` is not a cluster-wide hard cap, and
important build, load, query, and maintenance allocations are not yet governed
by one byte budget.

| ID | Issue | Status | Notes |
|---|---|---|---|
| KI-012 | Build preflight is not a hard memory bound; `oom_action = 'readonly'` still performs the over-budget construction. | P0 architecture fix | Low-memory rebuild reduces old/new overlap, but filters, resolution, both CSR directions, persistence buffers, stale row estimates, and PostgreSQL temp memory can exceed the estimate. Add a single remaining-memory governor, adaptive byte batches, and out-of-core spill. |
| KI-013 | mmap load, GQL/path execution, sync, projection reads, compaction, and analytics can allocate outside the build guard. | P0 architecture fix | Persist/mmap inbound CSR and large filter metadata; add query work/byte breakers, lazy scans/neighbor merges, bounded hydration and blocking operators, pinned projection snapshots, and range compaction. |
| KI-014 | GQL checks required mapped edge-table ACLs before execution and checks source-node and mapped relationship rows before coordinate, hydrated, aggregate, existence, join, wildcard-path, and relationship-list output, including durable mutable-overlay relationships. | Implemented on PostgreSQL 17; release matrix pending | PostgreSQL 14-16 and 18 evidence remains before retirement. Visibility stays enforced before topology is observable. |
| KI-015 | Parallel relationship rows retain distinct source identities through the base CSR, GQL paths and hydration, transaction overlays, trigger sync, durable ingestion and reload, identity-specific deletion, compaction, and dirty-range chunk replacement. | Implemented on PostgreSQL 17; release matrix pending | PostgreSQL 14-16 and 18 evidence remains before retirement. Endpoint-only fallback stays disabled for mapped relationship hydration and deletion. |
| KI-016 | Filter indexes use registered PostgreSQL table OID plus attribute identity throughout build, pushdown, and sync. | Implemented on PostgreSQL 17; release matrix pending | Same-named columns remain distinct internally, while ambiguous unscoped public filters fail with guidance instead of selecting an arbitrary table. PostgreSQL 14-16 and 18 evidence remains before retirement. |
| KI-017 | Projection publication now shares the graph-scoped PostgreSQL transaction advisory lock with build and vacuum, covering manifest read, node allocation, validation, and publication across backends. | Implemented on PostgreSQL 17; release matrix pending | The process-local lock remains a defensive in-process guard. PostgreSQL 14-16 and 18 evidence remains before retirement. |
| KI-018 | Persisted replacement is renamed over the serving path before mmap reload validation. | P0 durability fix | Write and validate a generation-specific artifact, catch it up, then atomically switch a small manifest while retaining the previous generation. |
| KI-021 | Error reporting previously called PostgreSQL `errfinish()` directly below pgrx's guarded error boundary. | Implemented on PostgreSQL 17; release matrix pending | Errors now use pgrx `ErrorReport`, standard PostgreSQL SQLSTATEs, and stable `PGxxx` diagnostics in `DETAIL`. A PostgreSQL-backed regression proves live Rust destructors run before the error reaches PL/pgSQL; PostgreSQL 14-16 and 18 evidence remains before retirement. |
| KI-022 | Durable filter projection deltas previously narrowed signed, temporal, large, and UUID values to `Option<u32>`. | Implemented on PostgreSQL 17; release matrix pending | Projection segment v5 stores tagged signed numeric, boolean, text, date, timestamp-with-time-zone, UUID, SQL `NULL`, tombstone values, and identity-bearing relationship topology/weight rows. Unit and PostgreSQL-backed tests cover sync, segment validation, reload, exact matching, and old-version rejection; PostgreSQL 14-16 and 18 evidence remains before retirement. |
| KI-023 | Extension-defined and dynamically generated trigger-sync security-definer functions pin `pg_catalog` before explicit `pg_temp`. | Implemented on PostgreSQL 17; release matrix pending | Catalog metadata audits cover both function families. Hostile persistent or temporary schemas cannot intercept generated trigger execution or shadow catalog relations during admin checks. PostgreSQL 14-16 and 18 evidence remains before retirement. |
| KI-024 | Registered relation identity is verified on PostgreSQL 17; supported-major and concurrent-DDL evidence remains. | Implemented on PostgreSQL 17; release matrix pending | Registrations store PostgreSQL OIDs, derive names only for display or quoted SQL, survive rename and search-path changes, and fail closed after a drop/recreate. |

## P1: Transaction And Performance Follow-Ups

These items can affect transaction behavior, latency, or memory pressure, but
do not change the intended public SQL contract.

| ID | Issue | Status | Notes |
|---|---|---|---|
| KI-005 | Overlay neighbor merging and transaction-delta lookups may be material traversal hot paths on dirty mutable projections. | Measure and close | Benchmark degree, segment depth, transaction inserts/deletes/tombstones, direction, weights, allocations, and p95/p99 with correctness checks. Optimize material regressions or close with an approved regression threshold and retained gate. |
| KI-025 | Background-worker jobs can return a Rust `GraphResult::Err` normally from a pgrx transaction closure after mutating job state, making partial commit behavior accidental. | P1 transaction fix | Centralize worker transaction policy, distinguish atomic work from intentional progress checkpoints, inject failures in every phase, and prove no unintended commit or stuck `running` job. |

## P2: SQL Ergonomics and Documented Semantics

These items clarify behavior or broaden the API. They are planned, but should
not block use of the current documented SQL surface.

| ID | Issue | Status | Notes |
|---|---|---|---|
| KI-006 | `graph.cypher()` is a narrow openCypher compatibility preview over pgGraph's supported GQL subset, not full openCypher compatibility. | Documented limitation | Keep compatibility rows honest and route broader openCypher expansion through [Roadmap](./roadmap), not this register. |

## Resolved Items Awaiting Retirement

These items are no longer active work. After their release-matrix regressions
are confirmed and release notes remain the durable history, remove them from
this page rather than carrying resolved rows indefinitely.

| ID | Resolution | Retirement evidence |
|---|---|---|
| KI-003 | Dedicated GQL and Cypher parser fuzz targets and seed corpora exist. | Targets build and scheduled/release fuzz policy owns continuing campaigns. |
| KI-011 | Metadata encoding moved to bincode 2 serde with strict trailing-byte rejection and a documented artifact rebuild requirement. | Upgrade/rebuild/corruption regressions green; migration remains in release notes. |

## Roadmap-Owned Future Work

Do not use this known-issues register for broad feature wishes that are not
current limitations. Track these in [Roadmap](./roadmap) instead:

- complete ISO GQL syntax and semantics beyond the current subset;
- costed multi-pattern planning, streaming execution, relationship property
  expressions, and optional path-variable projection semantics;
- a dedicated many-to-many relationship registration helper if post-fix user
  reports show `graph.add_edge()` remains too easy to misuse;
- PostgreSQL 19 SQL/PGQ catalog and `GRAPH_TABLE` interoperation;
- broader openCypher compatibility;
- semantic-guided search and pgVector ranking;
- WAL-driven sync;
- COPY build scanning;
- additional graph analytics;
- distributed graph execution.

## Lower-Priority Optimizations

These are intentionally deferred until measurement shows they matter:

- Replacing bitmap serialized-size estimates with native roaring helpers if the
  pinned crate exposes them.
- Reducing low-level clone counts in builder, traversal, weighted path, and
  aggregation paths.
- Combining duplicate path reconstruction passes after the larger traversal
  allocation work is complete.
- Cleaning up long internal function signatures when the surrounding traversal
  parameters stabilize.
- Cleaning up padding-bit handling in `active_count()`.

Canonical enums/newtypes, native UUID/OID boundaries, checked conversions, and
unsafe proof are active correctness work, not benchmark-dependent
micro-optimizations.
